{
  "name": "zaiko-cloudflare-container-router",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "wrangler dev --config wrangler.example.jsonc",
    "deploy:blocked": "echo \"Deployment requires explicit approval\" && exit 1"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.26.1"
  }
}
